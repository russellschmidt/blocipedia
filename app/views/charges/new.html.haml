.row
  = form_tag charges_path do
    %h4 Click the button!
    %script class='stripe-button' src="https://checkout.stripe.com/checkout.js" data-key="#{@stripe-btn-data[:key]}" data-amount=#{@stripe_btn_data[:amount]} data-description="#{@stripe_btn_data[:description]}"
